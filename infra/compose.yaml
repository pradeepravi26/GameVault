services:
  mysql:
    image: mysql:8.4
    container_name: gamevault-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: game_vault
    ports:
      - "3306:3306"
